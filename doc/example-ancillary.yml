---
main:
  port: 8880
  ssl-port: 8443
  interval: 30
  keystore: doc/keystore
  key-password: password
  hmac-secret: doc/hmac-secret
  logfile: /home/thawes/programs/var/ancillary.log
  loglevel: DEBUG

alerting:
  pagerduty:
    api-key: oksnfkwemwemwfmwe

endpoints:
  - health:
      - check:
          command: echo "iamok"
          secure: no
  - kafka:
      - testcheck:
          command: echo "Kafka!"
          secure: yes
      - testcheck:
          command: cat /etc/motd
          secure: no
      - nosy:
          command: cat /etc/group
          secure: yes
